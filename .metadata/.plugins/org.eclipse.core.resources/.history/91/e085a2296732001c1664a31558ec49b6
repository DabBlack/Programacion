package examenes.Examen20211021;

public class Ejercicio_C {

	public static void main(String[] args) {
		
		int num;
		
		for (int i = 0; i < 10; i++){
			
		
		num = Math.round(Math.random() * (limiteSuperior - limiteInferior)) + limiteInferior;
		(Math.round(Math.random() * (limiteSuperior - limiteInferior)) + limiteInferior)
		if (num % 2 == 0)
			System.out.println("Numero generado: " + num);
		
		}
		
	}

}
